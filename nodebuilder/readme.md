# Nodebuilder
Use as a building stage of your node app. The image contains python(v2) and node-gyp to build native addons. Nodebuilder uses the [official docker image](https://hub.docker.com/_/node) for node (alpine) as the base image.
